%%%%%%%%%%%%%%%%%%%%%%%%%%
% Wichtige Mengensysteme %
%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcommand{ \idClo }{ {\sssfont{C}} }
\newcommand{ \idOpn }{ {\sssfont{O}} }
\newcommand{ \idCpt }{ {\sssfont{K}} }
\newcommand{ \idRCpt }{ {\rmr\sssfont{K}} }
\newcommand{ \idPCpt }{ {\rmp\sssfont{K}} }
\newcommand{ \idBnd }{ {\sssfont{B}} }

\newcommand{ \ssClo }{ \operatorname{\sssfont{S}} }    % abgeschlossene Mengen
\newcommand{ \ssOpn }{ \operatorname{\sssfont{O}} }    % offene Mengen
\newcommand{ \ssCpt }{ \operatorname{\sssfont{K}} }    % kompakte Mengen
%\newcommand{ \ssPCpt }{ \operatorname{\sssfont{T}} }   % präkompakte Mengen
%\newcommand{ \ssPCpt }{ \operatorname{{\sssfont{K}_{\rmp}}} }   % präkompakte Mengen
\newcommand{ \ssPCpt }{ \operatorname{{\rmp\sssfont{K}}} }   % präkompakte Mengen
\newcommand{ \ssBnd }{ \operatorname{\sssfont{B}} }    % beschränkte Mengen
\newcommand{ \ssSim }{ \operatorname{\sssfont{S}} }    % Simplexbornologie
\newcommand{ \ssSSim }{ \operatorname{\sssfont{S}^{\opSol}} }    % beschränkte Mengen


%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Topologische Inklusionen %
%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcommand{ \csubs }{ \Subset }                                   % is eine kompakte Teilmenge von...
\newcommand{ \csups }{ \Supset }                                   % is eine kompakte Teilmenge von...


%\newcommand{ \topclsr }[ 1 ]{ \overline{ #1 } }
%\newcommand{ \topclsrin }[ 2 ]{ \overline{ #1 }{}^{ #2 } }
\newcommand{ \clTop }[ 1 ]{ \overline{ #1 } }
\newcommand{ \clTopin }[ 2 ]{ \overline{ #1 }{}^{ #2 } }


\newcommand{ \idCptSupp }{ {\mathrm{cs}} }
\newcommand{ \idLoc }{ {\mathrm{loc}} }


\newcommand{ \interior }{ \operatorname{int} }